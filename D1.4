
import java.util.* ;
import java.io.*; 

public class Solution {
	
	public static long maxSubarraySum(int[] a, int n) 
	{
		long max = Long.MIN_VALUE;
		long s=0;
		for(int i=0;i<n;i++)
		{
			s=s+a[i];
			if(s<0)
			s=0;
			max=Math.max(s,max);
		}
		return max;
	}

}
